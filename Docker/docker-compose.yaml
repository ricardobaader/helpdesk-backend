version: '3'
services:
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      MSSQL_SA_PASSWORD: VeryStr0ng!
      ACCEPT_EULA: 'Y'
    ports:
      - '1433:1433'

volumes:
  postgres:
